[package]
name = "grpc"
version = "0.9.0"
edition = "2021"
authors = ["gRPC Authors"]
license = "Apache-2.0"

[dependencies]
url = "2.5.0"
tokio = { version = "1.37.0", features = ["sync", "full" ] }
tonic = "0.11.0"
