[package]
name = "compile"
edition = "2021"
license = "MIT"

[dependencies]
prost = "0.14"

[dev-dependencies]
static_assertions = "1"
trybuild = "1"
tonic = {path = "../../tonic"}

[build-dependencies]
tonic-build = {path = "../../tonic-build"}
