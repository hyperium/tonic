[package]
name = "root-crate-path"
version = "0.1.0"
authors = ["Lucio Franco <luciofranco14@gmail.com>"]
edition = "2018"
publish = false
license = "MIT"

[dependencies]
tonic = { path = "../../tonic" }
prost = "0.7"

[build_dependencies]
tonic-build = { path = "../../tonic-build" }
