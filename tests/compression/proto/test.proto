syntax = "proto3";

package test;

import "google/protobuf/empty.proto";

service Test {
  rpc CompressOutput(google.protobuf.Empty) returns (SomeData);
  rpc CompressInput(SomeData) returns (google.protobuf.Empty);
}

message SomeData {
  // include a bunch of data so there actually is something to compress
  bytes data = 1;
}
